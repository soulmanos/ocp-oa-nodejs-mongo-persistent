---
ansible_connection: local
openshift_cluster_content:
- object: projects
  content:
  - name: dev
    template: "{{ playbook_dir }}/templates/project-request.yml"
    action: create
    params_from_vars: "{{ project_params_dict }}"
    tags:
    - projectrequests
    - projectrequests-dev
